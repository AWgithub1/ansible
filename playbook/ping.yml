- name: Ping host 10.254.17.10
  #hosts: 10.254.17.10
  hosts: localhost
  connection: ssh

  tasks:
    - name: Ping the target host
      ansible.builtin.ping:
      #ping:
        #host: 192.168.88.131
        #count: 1000
        #interval: 1

  #vars:
    #pingstack:
      #ansible_host: 10.254.17.10
